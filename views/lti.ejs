<!DOCTYPE html>
<html lang="en">
    <head>
        <title><%= title %></title>
        
        <meta charset="utf-8">
        <meta name="description" content="LTI 1.1 Tool consumer">
        <meta name="viewport" content="width=device-width, initial-scale=1">
    
        <base href="/">
    </head>
    <body>
        <form id="ltiForm" name="ltiForm" action=<%= action %> method=<%= method %>>
            <% Object.keys(formFields).forEach(function(key) { %>
                <input name=<%= key %> value=<%= formFields[key] %>>
            <% }); %>
        </form>
        <script>
            setTimeout(()=>{
                document.getElementById("ltiForm").submit();
            },5000)
        </script>
    </body>
</html>